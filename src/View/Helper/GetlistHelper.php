<?php 
namespace App\View\Helper;
use Cake\View\Helper;
use Cake\Datasource\ConnectionManager;
use Cake\ORM\TableRegistry;


class GetlistHelper extends Helper{
 	  public function showList($id,$type){
 	  	//$states = TableRegistry::get('States')-;
 	//   	$cities= TableRegistry::get('Cities')-;
 	 	//if($type=='city'){
		 //	$query = $states->find("all")->select('id','name')->where('$id');
		// }else{
		// 	$query = $cities->find("all")->select('id','name')->where('$id');
		// }
 	//     foreach ($query as $row) {
  //        	return   $row->name;
           		
		//  }        
    }
}
?>



